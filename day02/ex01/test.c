#include <stdio.h>

int sum(int a, int b)
{
	int r;
	r = a + b;
	// return (r);
}
int main()
{
	int a = -110, b = -199 , result;
	printf("%d\n", result);
	// result = sum (a, b);
	// printf("a is : %d, and b is : %d , result is : %d\n", a,b , result);
}